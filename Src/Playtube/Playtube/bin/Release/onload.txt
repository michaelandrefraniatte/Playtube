await checkSize("https://img.youtube.com/vi/F9H88N0sF9s/mqdefault.jpg");

async function checkSize(src) {
    var imgblob = await fetchBlob(src);
    return parseInt(parseInt(imgblob.size));
}

async function fetchBlob(url) {
    const response = await fetch(url);
    return response.blob();
}